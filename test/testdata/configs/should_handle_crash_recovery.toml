[general]
timeout = "150ms"

[[handler]]
on = "windowtitlev2"
when = "(.*),Crash"
then = "exit 1"

[[handler]]
on = "windowtitlev2"
when = "(.*),Regular"
then = "touch $TMP_TST_FILE_0 && echo $REGEX_GROUP_0 >> $TMP_TST_FILE_0"
